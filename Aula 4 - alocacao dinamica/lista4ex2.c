/* 2. Elabore um programa que simule a memória de um computador. O usuário irá especificar o tamanho 
da memória, ou seja, quantos bytes serão alocados do tipo inteiro (a memória deve ser inicializada 
com todos os dados zerados). Assim, a memória solicitada deve ser um valor múltiplo do tamanho 
do tipo inteiro. A seguir, o usuário terá duas possibilidades mediante um menu: inserir um valor em 
uma determinada posição ou consultar o valor contido em uma determinada posição. */

#include<stdio.h>
#include<stdlib.h>


